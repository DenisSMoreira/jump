{
  "name": "skybound-mobile-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Local mobile MVP verification for Skybound.",
  "scripts": {
    "check": "node --check enhancements.js && node --check sw.js && node --check tests/smoke.mjs",
    "test": "node tests/smoke.mjs",
    "verify": "npm run check && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
